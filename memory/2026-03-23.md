# 2026-03-23

- Mike asked to set up the mission control dashboard.
- Created a static `mission-control/` dashboard scaffold with HTML, CSS, JS, and README.
- Upgraded the dashboard with a local Python API (`server.py`) so it shows live workspace, git, OpenClaw status, and recent memory data.
- Added reminders, calendar, projects, service health, quick actions, and a config file to make mission control actually useful.
- Prepared local Google Calendar integration scripts for OAuth setup and creating/reusing a calendar named `OpenClaw` for `michael.coen@gmail.com`.
- Wired mission control to automatically read upcoming events from Google Calendar using the saved token/config, with fallback to static config entries if Google fetch fails.
- Switched the dashboard to use Mike's primary Google Calendar instead of the separate `OpenClaw` calendar.
- Fixed the frontend calendar widget and disabled asset caching so the live Google Calendar data actually appears in the UI after refresh/restart.
- Hardened the frontend against missing/stale DOM elements after a null-element crash in the calendar widget.
- Replaced a corrupted `app.js` frontend file that was leaving the calendar widget stuck on "Loading".
- Cleaned up mission control config/docs: removed placeholder calendar entries, added gitignore for local Google auth artifacts, and increased Google Calendar lookahead to 30 days.
- Added a polish pass: human-friendly event times, deduped obvious ticket/event duplicates, and a top-level next-event card.
